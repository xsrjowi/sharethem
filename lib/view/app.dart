import 'package:flutter/material.dart';
import 'package:sharethem/view/home/homepage.dart';

// Todo : Control de redirecciones (inicio de sesión)
class ShareThem extends StatelessWidget {
  const ShareThem({super.key});

  @override
  Widget build(BuildContext context) {
    return const HomePage();
  }
}
